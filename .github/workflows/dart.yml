name: Dart CI

on:FEATURE
  push:FEATURE<HELLO_WORLD>
    branches: [ master ]
  pull_request:Dart.API
    branches: [ master ]

jobs:**+!echo >
  build:mas6

    runs-on: ubuntu-latest

    container:master_feature

      image:  google/dart:latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: pub get
    - name: Run tests
      run: pub run test
